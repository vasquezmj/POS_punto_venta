<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.Font?>

<BorderPane xmlns="http://javafx.com/javafx/17"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="com.sellcontrol.controller.DashboardController"
            styleClass="dashboard-container">

    <!-- Barra Superior -->
    <top>
        <HBox alignment="CENTER_LEFT" spacing="20" styleClass="toolbar">
            <padding>
                <Insets top="15" right="20" bottom="15" left="20"/>
            </padding>
            <VBox>
                <Label fx:id="lblBienvenida" text="Bienvenido" styleClass="toolbar-title">
                    <font><Font size="18"/></font>
                </Label>
                <Label fx:id="lblRol" text="Rol:" styleClass="toolbar-subtitle"/>
            </VBox>
            <Region HBox.hgrow="ALWAYS"/>
            <Button text="Cerrar Sesión" onAction="#handleCerrarSesion" styleClass="btn-danger"/>
        </HBox>
    </top>

    <!-- Contenido Central: Menú de opciones -->
    <center>
        <VBox alignment="CENTER" spacing="15">
            <padding>
                <Insets top="30" right="40" bottom="30" left="40"/>
            </padding>
            <Label text="Panel Principal" styleClass="section-title">
                <font><Font size="22"/></font>
            </Label>

            <HBox alignment="CENTER" spacing="15">
                <padding>
                    <Insets top="20"/>
                </padding>

                <!-- Botones del menú -->
                <Button fx:id="btnUsuarios" text="👥  Usuarios" onAction="#handleUsuarios"
                        styleClass="btn-menu" prefWidth="180" prefHeight="120"/>

                <Button fx:id="btnProductos" text="📦  Productos" onAction="#handleProductos"
                        styleClass="btn-menu" prefWidth="180" prefHeight="120"/>

                <Button fx:id="btnVentas" text="🛒  Ventas" onAction="#handleVentas"
                        styleClass="btn-menu" prefWidth="180" prefHeight="120"/>
            </HBox>

            <HBox alignment="CENTER" spacing="15">
                <Button fx:id="btnCaja" text="💰  Caja" onAction="#handleCaja"
                        styleClass="btn-menu" prefWidth="180" prefHeight="120"/>

                <Button fx:id="btnGastos" text="📋  Gastos / Merma" onAction="#handleGastos"
                        styleClass="btn-menu" prefWidth="180" prefHeight="120"/>

                <Button fx:id="btnReportes" text="📊  Reportes" onAction="#handleReportes"
                        styleClass="btn-menu" prefWidth="180" prefHeight="120"/>
            </HBox>

            <HBox alignment="CENTER" spacing="15">
                <Button fx:id="btnTicketConfig" text="🎫  Ticket" onAction="#handleTicketConfig"
                        styleClass="btn-menu" prefWidth="180" prefHeight="120"/>
            </HBox>
        </VBox>
    </center>

</BorderPane>
