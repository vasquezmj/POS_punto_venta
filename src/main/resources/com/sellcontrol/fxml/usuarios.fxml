<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.Font?>

<BorderPane xmlns="http://javafx.com/javafx/17"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="com.sellcontrol.controller.UsuarioController"
            styleClass="dashboard-container">

    <!-- Barra Superior -->
    <top>
        <HBox alignment="CENTER_LEFT" spacing="15" styleClass="toolbar">
            <padding>
                <Insets top="12" right="20" bottom="12" left="20"/>
            </padding>
            <Label text="Gestión de Usuarios" styleClass="toolbar-title">
                <font><Font size="18"/></font>
            </Label>
            <Region HBox.hgrow="ALWAYS"/>
            <Button text="← Volver" onAction="#handleVolver" styleClass="btn-secondary"/>
        </HBox>
    </top>

    <!-- Tabla de Usuarios -->
    <center>
        <VBox spacing="10">
            <padding>
                <Insets top="15" right="20" bottom="10" left="20"/>
            </padding>

            <TableView fx:id="tablaUsuarios" VBox.vgrow="ALWAYS" styleClass="data-table">
                <columns>
                    <TableColumn fx:id="colId" text="ID" prefWidth="50"/>
                    <TableColumn fx:id="colNombre" text="Nombre" prefWidth="170"/>
                    <TableColumn fx:id="colUsuario" text="Usuario" prefWidth="120"/>
                    <TableColumn fx:id="colRol" text="Rol" prefWidth="90"/>
                    <TableColumn fx:id="colActivo" text="Activo" prefWidth="70"/>
                    <TableColumn fx:id="colCreado" text="Creado" prefWidth="150"/>
                </columns>
            </TableView>
        </VBox>
    </center>

    <!-- Barra inferior: botones de acción -->
    <bottom>
        <HBox alignment="CENTER" spacing="10" styleClass="action-bar">
            <padding>
                <Insets top="10" right="20" bottom="15" left="20"/>
            </padding>
            <Button text="➕ Nuevo" onAction="#handleNuevo" styleClass="btn-primary"/>
            <Button text="✏️ Editar" onAction="#handleEditar" styleClass="btn-secondary"/>
            <Button text="🔑 Cambiar Contraseña" onAction="#handleCambiarPassword" styleClass="btn-secondary"/>
            <Button text="🔄 Activar/Desactivar" onAction="#handleToggleActivo" styleClass="btn-warning"/>
            <Button text="🗑️ Eliminar" onAction="#handleEliminar" styleClass="btn-danger"/>
            <Region HBox.hgrow="ALWAYS"/>
            <Label fx:id="lblMensaje" text="" styleClass="status-label"/>
        </HBox>
    </bottom>

</BorderPane>
